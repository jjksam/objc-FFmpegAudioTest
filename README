Test app which have background audio play back using FFmpeg
play、pause、stop

iOS Audio Queue Services was used, I’ve tested in iPhone/iPad.

To Build the ffmpeg library, run the following command in the directory.
./build_universal

FLV file (file format that containing MP3) is read
using ffmpeg to decode the data to PCM16LE for iOS Audio Queue service play back.
